{
  "name": "hireshield",
  "version": "0.1.0",
  "description": "Open-source AI that detects suspicious job postings, ghost jobs, and recruitment phishing — without accusing specific employers.",
  "license": "MIT",
  "keywords": [
    "job-scam-detection",
    "ghost-jobs",
    "recruitment-fraud",
    "phishing-detection",
    "nlp",
    "openai",
    "trust-score",
    "typescript"
  ],
  "scripts": {
    "preinstall": "sh -c 'rm -f package-lock.json yarn.lock; case \"$npm_config_user_agent\" in pnpm/*) ;; *) echo \"Use pnpm instead\" >&2; exit 1 ;; esac'",
    "build": "pnpm run typecheck && pnpm -r --if-present run build",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run typecheck",
    "test": "pnpm -r --if-present run test"
  },
  "private": true,
  "devDependencies": {
    "playwright": "^1.60.0",
    "prettier": "^3.8.3",
    "typescript": "~5.9.3",
    "vitest": "^4.1.9"
  }
}
